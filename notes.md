# cart logic

- all the core logic for the cart items state exists in the use-cart.ts (zustand store)
- add-to-cart-button.tsx component contains logic for altering the cart state
- cart-sync.tsx component contains logic to sync cart state between the client and the server
